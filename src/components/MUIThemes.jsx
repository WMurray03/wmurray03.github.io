import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#f44336",
    },
    action: {
      disabledBackground: "#d3d3d3",
      disabled: "#5A5A5A",
    },
  },
});
